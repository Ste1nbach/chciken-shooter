export {canvas, wrap, btn, ctx, array};

const canvas = document.querySelector("canvas");
const wrap = document.getElementById("wrap");
const btn = document.getElementById("btn");
const ctx = canvas.getContext("2d");
let array = [];
